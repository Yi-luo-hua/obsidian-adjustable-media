# 0.4.0 后续修复验证

日期：2026-09-21。修复保存在 `codex/fix-layout-scroll`，尚未发布。

## 自动检查

`npm run check` 通过：类型检查、官方插件规则 lint、191 项测试。`npm run build` 通过。

新增回归覆盖相邻公式、图注段落边界（含无外侧竖线的表格）、不同对齐方式的缩放方向、布局手势独立撤销、失效锚点拒绝写入，以及普通文字输入保持原来的历史行为。

## Obsidian 测试库实测

仅使用 `vml-test-vault`。拖动、菜单、源码按钮、撤销和重做使用 DevTools 真实输入；视图对齐比较实际 DOM 尺寸。

| 场景 | 结果 |
| --- | --- |
| 长笔记中移动布局内图片，光标停在笔记开头 | 修复前滚动位置从约 3671 跳到 87；修复后滚动位置及布局顶部变化为 0 |
| 同一笔记双窗格移动、Ctrl+Z、Ctrl+Y | 内容正确恢复和重做，两个窗格滚动位置保持不变 |
| 图片两侧带文字，编辑/阅读视图 | 宽布局约 938px 时高度均为 342.205px；文字栏宽度、图片相对位置一致 |
| 同样布局缩窄到约 431px | 两种视图高度均为 574.262px；图片位置和尺寸一致 |
| 点击“编辑源码” | 保留只读布局预览，图片位置不变，原始文本在其下展开 |
| 点击文字栏进入编辑后按 Esc | 图片位置不变 |
| 靠右/居中图片缩放、skip=3 | 缩放方向正确；两视图间隔高度一致 |
| 图注弹窗打开后切换到相同内容的另一篇笔记 | 拒绝写入，两篇内容均未改变 |
| 浮动图片旁的宽表格逐段进入阅读视图 | 修复前约 402px 跳动；相同路径复测消失 |
| 宽表格横向滚动与插件卸载 | 容器宽 539px、内容宽 704px，横向滚动可达 40px；卸载后包装容器及样式类均清理为 0 |
| 阅读视图两篇长笔记每步 40px 扫描 | 共 424 步，无超过 2px 的可见段落异常位移，无新增运行错误 |

源码展开会增加笔记高度；验证保证保留图片预览，不保证展开后整篇文档总高度不变。以上结果限于测试库的当前 Obsidian 和主题，不能替代其他主题、版本的兼容性检查。

## 尚未解决的快速滚动边界

每步 120px、间隔 45ms，从长笔记底部往返扫描时，`wrap-test.md` 的实时预览仍出现可见段落位移：稳定路径包含约 48px 位移，部分重排会在后续段落累积。`wrap-multi.md` 的阅读视图首次扫描曾出现约 24–48px 位移，后续两次重复扫描未出现；这不视为完全修复。

这些发生于虚拟渲染区域切换，区别于已经实测修复的布局写入、撤销和源码按钮触发的跳动。提前离屏测量全部浮动图片的尝试没有改善整体结果，反而增加重排，因此未保留。后续需同步追踪 CodeMirror 高度估算、占位与浮动替身进入视口的时序，不能仅用是否存在图片尺寸缓存判定根因。

测试使用独立临时笔记。原有笔记哈希核对发现 `对照 普通嵌入.md` 与 `test1.md` 相对最初快照发生变化；本轮测试没有向这两篇写入，保留当前内容，不用旧快照覆盖。
