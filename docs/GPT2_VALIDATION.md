# GPT-2 v3 三种视图对比

2026-09-22，在专用测试库 `vml-test-vault` 中对比 `GPT-2 复刻 前5页 v3.md` 的实时预览、阅读模式和用户提供的同名 PDF；用修复后的插件从 Obsidian 原生导出流程生成对照 PDF。

| 问题 | 原因与修复 | 验证结果 |
| --- | --- | --- |
| 编辑与阅读模式文字间距、分栏高度不同 | 统一布局文字中的分隔线和 MathJax 外边距；阻止正文浮动规则泄漏到布局内部 | 同一窗口宽度下，9 个布局块的宽、高差值均为 0px |
| 实时预览 callout 显示原始引用 | callout 后处理没有段落行号；增加局部编号处理，并在编辑后刷新保留的部件 | 已知引用正确编号，删除前面的标签后“图 2”更新为“图 1”；行内代码未改变 |
| PDF 块外引用、公式标签未处理 | 导出占位正文渲染前，使用完整笔记的索引完成编号 | 块内外引用、图表标题和公式编号均保留；未知标签仍显示 `??` |
| PDF 宽表格右侧被裁切 | 导出表格限制在可用宽度内，宽表使用紧凑字号和内边距 | 11 列全部可见，最后一列 1BW 和数值完整，SOTA、1542M 行名不再拆行 |
| 大文字块造成多余分页留白 | 允许非浮动纯文字布局跨页，浮动旁注继续保持整块 | 原 PDF 11 页，对照 PDF 10 页；末页左右旁注完整 |

## 验证范围

- 同宽比较的正文容器约为 938.40px；对照 PDF 使用原生 Letter 纵向纸张。纸张宽度与连续编辑画布不同，仍会自然换行和分页，不能保证逐像素一致。
- 检查原生导出的页面图像、公式、图表、宽表和浮动旁注；新增导出编号回归测试。
- `npm run check` 通过类型检查、lint 和全部 195 项测试；`npm run build` 通过生产构建。
- 原始 Markdown 和 PDF 通过 SHA-256 与操作前备份核对，内容未改动。
- 临时 callout 回归笔记用于验证编号更新，完成后清理。
- 此验证针对当前测试库主题和 Obsidian 环境，不代表所有主题、纸张尺寸或超长不可分割内容。

本地测量、截图和对照 PDF 保存在 `E:\Cache\11-temp\codex-gpt2-compare-20260921`，不随仓库发布。
